using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace ConsoleApplication1
{
           

    class A
    {
        public int funcA(int temp)
        {
            temp = temp * 2;
            return temp;
        }
    }

    class B
    {
        public int funcB(int compare, int startcal)
        {
            int repeatB = 0;
            do
            {
                startcal = startcal * 3;
                repeatB++;
            } while (compare >= startcal);
            Console.WriteLine(" B's func will be called " + repeatB);
            return startcal;
        }


    }

    class c
    {
        public void funcC(int compare, int startcal)
        {
            int repeatC = 0;
            do
            {
                startcal = startcal * 5;
                repeatC++;
            } while (compare == startcal);
            Console.WriteLine(" C's func will be called " + repeatC);

        }


    }

    class D
    {
        static void Main(string[] args)
        {
            int  NewValue, temp, countA=0, countB=0, countC=0;
            Boolean res=true ;
            Console.WriteLine("Enter New Value::");
            NewValue = int.Parse(Console.ReadLine());
            temp = NewValue;
            A objA = new A();
            B objB = new B();
            c objC = new c();

            if (NewValue == 1 || (NewValue % 2 != 0 && NewValue % 3 != 0 && NewValue % 5 != 0 ))
            {
                res = false;
            }
                
            else
            {

                do
                {
                    if (temp % 2 != 0 && temp % 3 != 0 && temp % 5 != 0)
                    {
                        res = false;
                        break;
                    }
                    else if (temp % 2 == 0)
                    {
                        temp = temp / 2;
                        countA++;
                    }
                    else if (temp % 3 == 0)
                    {
                        temp = temp / 3;
                        countB++;
                    }
                    else if (temp % 5 == 0)
                    {
                        temp = temp / 5;
                        countC++;
                    }
                    else
                    {
                        // res = true;
                        break;
                    }

                } while (temp != 1);
            }
            if (res == false)
            {
                Console.WriteLine("New Value is not only 2,3 and 5 as its prime factor");
            }
            else
            {
                Console.WriteLine("A's function is called: " + countA);
                Console.WriteLine("B's function is called: " + countB);
                Console.WriteLine("C's function is called: " + countC);   
            }
            
            Console.ReadLine();

        }

    }
}
